# A hash table (hash function + an array together)
book = dict()
# Maps keys to values
book["apple"] = 0.67
book["milk"] = 1.49
book["avocado"] = 1.49

print(book) # Prints keys (apple) and values (0.67)

print(book["apple"])